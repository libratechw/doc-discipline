---
name: doc-discipline
description: >-
  プロジェクト文書（README、docs/、仕様書、STATUS、コメント）の作成・更新・整理の規律。
  文書を書く/直すとき、コード変更後に文書を追従させるとき、文書の重複・陳腐化・
  肥大に対処するときに使う。Use when writing or updating project docs, syncing
  docs after code changes, or fighting doc rot, duplication, and stale references.
  Triggers: documentation, README, docs update, spec, changelog, stale docs.
---

# 文書の規律

文書はコードと同じ基準（理解しやすい、正しく変更しやすい、保守できる）で設計する。
放っておくと文書は腐り、実装と食い違った時点で害になる。

## 何を書くか

- **これから作業する人が知っている必要がある情報だけ**を書く。確定した仕様、契約、
  設計判断とその理由、環境の制約、外部情報源の出典。
- **Git 履歴を見れば分かることは書かない**。修正の経緯、過去のバグとその原因、
  フェーズや進行状況の記録、レビュー指摘とその対応、確認済みチェックリスト。
  - 線引き: **決定と理由は残す。決定に至る過程は残さない**。「X ではなく Y を採用。
    理由は Z」は書く（却下案の理由も現役の情報）。試行錯誤の時系列は履歴に任せる。
- **現在の事実へ上書きする**。「以前は X だったが今は Y」ではなく「Y」と書く。
  日付入りの追記は積まない。
- 時点が本文に固定されない継続文書では、「現行」「最新」「現在の候補」のように
  時間経過だけで参照先が変わる表現を使わない。役割はbranch名、測定結果や固定した
  事実はcommit hashで対象を特定する。commit messageやPull Requestなど時点固定の記録は除く。
- 事実と未確定を混ぜない。未確定は「未確定」として、何を測れば決まるかまで書く。
- コマンド例・JSON 例・設定例も仕様記述と同じく**陳腐化対象**。copy-paste される例は
  実際の CLI / API / contract に一致させる。動的に変わる件数・一覧・デフォルト値は
  例や README へ手書きで複製しない。
- **TODO / FIXME を進捗管理にしない**。将来タスクは issue / task tracker へ置き、
  コードや文書には現在の理解に必要な制約だけを書く。

## どう構成するか

- **1 文書 1 役割、1 事実 1 か所**。禁止するのは同じ事実の**独立した二重所有**
  （second source of truth）。ある事柄の正本（canonical owner）は 1 つに決め、
  他からは参照するだけ。正本から機械的に導出される投影（registry → 生成 config、
  正本 → 参照リンク、manifest → 検証済み出力）は複製ではなく、二重所有にならない。
- どの事柄がどの文書に属するかは、リポジトリの文書一覧（README の構成一覧など）が示す。
- **新しい文書を安易に作らない**。まず既存文書のどれに属するかを考える。どれにも
  属さない役割だと説明できるときだけ増やし、増やしたら文書一覧へ加える。
- 作業指示書、レビュー記録、検証手順書といった**一時的な文書をリポジトリへ
  残さない**。そこで確定した事実だけを正本へ反映し、文書自体は消す。
- **役割を失った文書は消す**。別の正本へ吸収された、誰も参照しない、内容の大半が
  履歴・進捗・移行メモになった文書は、事実を正本へ移して削除する。「念のため」は残さない。

## コードを変えたら文書を合わせる

**同じコミットの中で**整合を取る。後で直すと必ず忘れる。

1. 変更したのが仕様（操作、protocol、表示、契約）なら、対応する正本文書を更新する。
   実装だけ変えて文書を放置しない。
2. 変更したファイル・関数・仕様を参照している箇所を洗い出して直す。

   ```bash
   git grep -n "変更した名前"        # 文書とコメントの両方を見る
   ```

3. 文書を削除・改名したら、参照しているコメントと文書を全部付け替え、
   リンク切れがないことを確認する。
4. コードのコメントと docstring は、コードから明らかな what / how を言い換えない。
   コードだけでは残らない why / invariant / external constraint / safety reason を
   書く。内容に別の正本があるなら本文を複製せず参照する（複製した瞬間から二重管理になる）。
5. プロジェクトに現在地を示す文書（STATUS など）があれば、作業の終わりに更新する。
   確認待ちが解消したらその項目を消し、結果は該当する正本へ反映する。

## 実装と文書が食い違っていたら

食い違いの種類で対応を分ける:

- **仕様・契約の食い違い**（文書の言う挙動と実装の挙動が違う): どちらが正しいかは
  実装者が決めることではない。黙って書き換えず、食い違いを記録してユーザーへ確認する。
- **事実記述のずれ**（リネーム・移動・削除への未追従、誤字、リンク切れ、
  実装から機械的に確認できる記述): 判断の余地がないのでその場で直す。
